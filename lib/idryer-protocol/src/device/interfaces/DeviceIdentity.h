/**
 * @file DeviceIdentity.h
 * @brief Совместимость со старым include пути.
 * @deprecated Используйте `core/types.h`.
 */

#pragma once

#include "../../core/types.h"

// Legacy alias.
using DeviceIdentity = idryer::DeviceIdentity;
